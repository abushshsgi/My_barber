# MyBarber

Backend: `backend/` (Django). Frontend to‘rtta alohida Next.js ilova: `apps/user-web`, `apps/admin-web`, `apps/barber-web`, `apps/barber-panel-web` (yangi UI/UX barber panel). Umumiy kutubxona: `packages/shared`.

Qisqa qo‘llanma: [apps/README.md](apps/README.md).

```bash
npm install
npm run dev:user
```

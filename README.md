# MyBarber

Backend: `backend/` (Django). Frontend web: `apps/user`, `apps/admin`, `apps/barber`. Mobil (Expo React Native): `apps/mobile`. Umumiy kutubxona: `packages/shared`.

Qisqa qo‘llanma: [apps/README.md](apps/README.md).

```bash
npm install
npm run dev:user
```
